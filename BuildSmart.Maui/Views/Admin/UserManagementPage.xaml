<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:BuildSmart.Maui.ViewModels.Admin"
             xmlns:gql="clr-namespace:BuildSmart.Maui.GraphQL"
             x:DataType="vm:UserManagementViewModel"
             x:Class="BuildSmart.Maui.Views.Admin.UserManagementPage"
             Title="User Management">

    <RefreshView Command="{Binding LoadUsersCommand}" IsRefreshing="{Binding IsBusy}">
        <Grid RowDefinitions="*">
            <CollectionView ItemsSource="{Binding Users}" Margin="10">
                <CollectionView.EmptyView>
                    <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center">
                        <Label Text="No users found." FontSize="18" TextColor="Gray" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="gql:IGetUsers_Users">
                        <Frame Margin="0,5" Padding="15" BorderColor="LightGray">
                            <Frame.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:UserManagementViewModel}}, Path=EditUserCommand}"
                                                      CommandParameter="{Binding .}"/>
                            </Frame.GestureRecognizers>
                            <Grid ColumnDefinitions="*, Auto">
                                <VerticalStackLayout Spacing="2">
                                    <Label FontAttributes="Bold" FontSize="16">
                                        <Label.FormattedText>
                                            <FormattedString>
                                                <Span Text="{Binding FirstName}" />
                                                <Span Text=" " />
                                                <Span Text="{Binding LastName}" />
                                            </FormattedString>
                                        </Label.FormattedText>
                                    </Label>
                                    <Label Text="{Binding Email}" FontSize="12" TextColor="Gray" />
                                    <Label Text="{Binding Role, StringFormat='{0}'}" FontSize="12" FontAttributes="Italic" TextColor="{StaticResource Primary}" />
                                </VerticalStackLayout>
                                
                                <Image Grid.Column="1" Source="chevron_right.png" HeightRequest="20" VerticalOptions="Center" />
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Grid>
    </RefreshView>
</ContentPage>
